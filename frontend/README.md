# AniList Sequel Finder - Frontend

This is the React frontend for the AniList Sequel Finder.

## 🚀 Getting Started

1.  **Install dependencies**:
    ```bash
    npm install
    ```

2.  **Run the development server**:
    ```bash
    npm run dev
    ```

3.  **Open in browser**:
    Visit [http://localhost:5173](http://localhost:5173)

## 🛠️ Tech Stack

-   **Vite**: Build tool
-   **React**: UI Library
-   **TypeScript**: Type safety
-   **Tailwind CSS**: Styling
-   **TanStack Query**: State management & Caching
-   **Axios**: API Client
-   **Lucide React**: Icons

## 🔗 Backend Connection

Ensure the backend is running on `http://localhost:8000`.
The API client is configured in `src/api/client.ts`.
